// This is just the structure - we'll add fields later when coding
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// We'll define models here later:
// User
// Group  
// UserGroup
// Match
// Vote
// MatchResult